import { IaCTestFlags, IacFileParsed, IacFileParseFailure, SafeAnalyticsOutput } from './types';
import { TestResult } from '../../../../lib/snyk-test/legacy';
import { IacFileInDirectory } from '../../../../lib/types';
export declare function test(pathToScan: string, options: IaCTestFlags): Promise<{
    results: TestResult | TestResult[];
    /** All files scanned by IaC with parse errors */
    failures?: IacFileInDirectory[];
}>;
export declare function removeFileContent({ filePath, fileType, failureReason, projectType, }: IacFileParsed | IacFileParseFailure): SafeAnalyticsOutput;
