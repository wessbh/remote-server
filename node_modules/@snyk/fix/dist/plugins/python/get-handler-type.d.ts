import { EntityToFix } from '../../types';
import { SUPPORTED_HANDLER_TYPES } from './supported-handler-types';
export declare function getHandlerType(entity: EntityToFix): SUPPORTED_HANDLER_TYPES | null;
export declare function isRequirementsTxtManifest(targetFile: string): boolean;
export declare function isPipfileManifest(targetFile: string): boolean;
